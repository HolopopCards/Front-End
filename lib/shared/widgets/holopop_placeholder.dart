import 'package:flutter/material.dart';

class HolopopPlaceholder extends StatelessWidget {
  const HolopopPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Expanded(
      child: Text("Coming soon!"),
    );
  }
}